export const Header = () => {
  return (
    <header className="flex items-center justify-between px-6 py-4">
      <nav>
        <span className="font-bold text-lg">
          f0 - Form Builder
        </span>
      </nav>
      <nav>

      </nav>
    </header>
  );
};